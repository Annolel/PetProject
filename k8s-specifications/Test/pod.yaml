apiVersion: v1
kind: Pod
metadata:
  labels:
    app: front-end
    size: mid
  name: my-nginx-yaml
spec:
  containers:
    - name: my-nginx-yaml
      image: nginx
      ports:
        - containerPort: 80